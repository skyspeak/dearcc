# Field Report → StayRelevant partner enroll
# Used by /api/subscribe (Vercel serverless). Never commit real secrets.

# Public base URL of the StayRelevant deployment (no trailing slash)
STAYRELEVANT_URL=https://stayrelevant.email

# Same value as StayRelevant's PARTNER_SECRET
STAYRELEVANT_PARTNER_SECRET=
