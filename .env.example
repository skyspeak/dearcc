# Field Report → dear[CC] The Letter enroll
# Used by /api/subscribe (Vercel serverless). Never commit real secrets.

# Public base URL of The Letter API (no trailing slash)
LETTER_URL=

# Shared partner enroll secret (blank until configured)
LETTER_PARTNER_SECRET=
